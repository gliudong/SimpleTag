# 如何启用 Auto-Edit 按钮

Auto-Edit 按钮只在**高级编辑器模式**下显示，并且只在编辑**单个文件**时可用。

## 步骤：

1. 打开 SimpleTag 应用
2. 进入 **Settings**（设置）
3. 找到 **"Simple Editor"** 选项
4. **关闭**这个选项（切换到高级编辑器）
5. 返回选择一个音乐文件
6. 点击编辑标签
7. 现在应该可以看到底部的工具栏，Auto-Edit 按钮在 Lyrics 按钮旁边（一个搜索+编辑图标）

## 高级编辑器工具栏布局：

```
[Lyrics] [Auto-Edit] [Add Field] [Save]
```

## 如果仍然看不到按钮：

请确认：
- ✅ 只选择了**一个**音乐文件（不是批量编辑）
- ✅ 已关闭"Simple Editor"选项
- ✅ 在底部可以看到水平工具栏（不是单个保存按钮）
